{
  "routes": [
    { "handle": "filesystem" },

    { "src": "/api/(.*)", "dest": "/api/server" },
    { "src": "/.well-known/(.*)", "dest": "/api/server" },

    { "src": "/(.*)", "dest": "/public/index.html" }
  ]
}
